{
  "$comment": "portfolio.json (shopify-app-kit): every product the cross-repo routines span. One entry per consumer repo: its name, `<owner>/<repo>`, the manifest path inside it, the cloud environment its routines fire in, and the routines it runs (file names under routines/ without .md). The kit-health routine iterates it; /shopify-app-kit:new-app's checklist appends the new app by hand. The single entry below is a placeholder; replace it, never keep it.",
  "products": [
    {
      "name": "Example App",
      "repo": "<owner>/<repo>",
      "manifest": ".claude/shopify-app.json",
      "environment": "<environment-name>",
      "routines": ["triage", "nuclear-review", "pr-steward", "kit-health", "graphify-refresh", "dependency-wave"]
    }
  ]
}
